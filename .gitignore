# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js / build
.next/
out/
build/
dist/

# Go build output
backend/bin/

# Runtime data (hosted sites, git repos, uploads)
/data/
*.log

# Backup artifacts produced by deploy/backup.sh (DB dumps + data tarballs may
# contain real site data/secrets — never commit them to the public repo).
deploy/backups/

# Env / secrets
.env
.env.*
!.env.example
# The e2e test env is COMMITTED and secret-free (throwaway localhost test values).
!e2e/.env.e2e

# Playwright e2e artifacts
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
e2e/playwright/.cache/

# necorox-specific deployment artifacts — keep OUT of the public OSS repo
deploy/cf-dns-backup-*.json
deploy/traefik-kotoji-dataplane.yml

# Docker
docker-compose.override.yml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local plans
.local/
