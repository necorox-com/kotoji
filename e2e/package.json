{
  "name": "kotoji-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end browser tests for kotoji's core user journey against a real composed stack.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
